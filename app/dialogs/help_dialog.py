"""Диалог справки / руководство пользователя."""

from PyQt6.QtWidgets import QDialog, QVBoxLayout, QTextBrowser, QPushButton
from PyQt6.QtCore import QSize

HELP_HTML = """\
<h2>Помощник загрузки — Руководство пользователя</h2>

<h3>Требования</h3>
<p>Для работы приложения необходимы три внешних инструмента. Они должны находиться
<b>в одной папке с приложением</b> (exe) или <b>в системном PATH</b>.</p>

<table border="1" cellpadding="6" cellspacing="0" style="border-collapse:collapse;">
<tr>
  <th>Инструмент</th>
  <th>Назначение</th>
  <th>Ссылка для скачивания</th>
</tr>
<tr>
  <td><b>N_m3u8DL-RE</b></td>
  <td>Загрузчик потокового видео (HLS/DASH)</td>
  <td><a href="https://github.com/nilaoda/N_m3u8DL-RE/releases">
      github.com/nilaoda/N_m3u8DL-RE/releases</a></td>
</tr>
<tr>
  <td><b>ffmpeg</b></td>
  <td>Мультиплексирование и обработка видео/аудио</td>
  <td><a href="https://ffmpeg.org/download.html">ffmpeg.org/download.html</a><br>
      <a href="https://github.com/BtbN/FFmpeg-Builds/releases">
      github.com/BtbN/FFmpeg-Builds/releases</a></td>
</tr>
<tr>
  <td><b>mp4decrypt</b></td>
  <td>Расшифровка защищённого контента (Bento4)</td>
  <td><a href="https://www.bento4.com/downloads/">bento4.com/downloads</a><br>
      <a href="https://github.com/nichengjian0729/bento4_mirror/releases">
      github.com/nichengjian0729/bento4_mirror/releases</a></td>
</tr>
</table>

<p style="margin-top:8px;">
При запуске загрузки приложение автоматически проверяет наличие всех трёх
утилит и выводит результат в логи. Если какая-то утилита не найдена —
загрузка не начнётся.</p>

<hr>

<h3>Вкладка «Новая задача»</h3>

<p><b>1. Получение команды</b><br>
Отправьте System Log (JSON) боту
<a href="https://t.me/kinpro_bot">@kinpro_bot</a> в Telegram.
Бот вернёт готовую команду для N_m3u8DL-RE. Скопируйте её целиком.</p>

<p><b>2. Вставка и разбор команды</b><br>
Вставьте скопированную команду в поле <i>«Команда»</i> и нажмите
<b>«Разобрать команду»</b>. Приложение автоматически извлечёт URL,
имя файла и другие параметры.</p>

<p><b>3. Имя файла</b><br>
После разбора имя файла появится в поле <i>«Имя файла»</i>.
Вы можете изменить его вручную или нажать <b>«Нормализовать»</b> —
кириллица и спецсимволы будут заменены на латиницу и подчёркивания
(например: «Тестовое Видео» &rarr; «Testovoe_Video»).</p>

<p><b>4. Место сохранения</b></p>
<ul>
  <li><b>Локально</b> — выберите папку кнопкой <b>«Обзор...»</b>.
      Последняя использованная папка запоминается.</li>
  <li><b>S3</b> — укажите путь в бакете. Нажмите <b>«Настройки»</b>,
      чтобы ввести параметры подключения (endpoint, бакет, ключи).
      Кнопка <b>«Проверить подключение»</b> в настройках позволяет
      убедиться, что всё работает.<br>
      При загрузке в S3 файл сначала скачивается локально во временную
      папку, затем отправляется в S3.<br>
      Галочка <b>«Удалить локальную копию после загрузки в S3»</b>
      автоматически удалит временный файл после успешной отправки.</li>
</ul>

<p><b>5. Повторы при ошибке</b><br>
Поле <b>«Повторы при ошибке»</b> (0–10, по умолчанию 2) задаёт количество
автоматических повторных попыток, если загрузка завершилась с ошибкой.</p>

<p><b>6. Очередь задач</b><br>
Кнопка <b>«Добавить в очередь»</b> позволяет накопить несколько задач.
После этого нажмите <b>«Скачать»</b> — задачи будут выполняться
последовательно одна за другой.<br>
Если очередь пуста, <b>«Скачать»</b> сразу запустит текущую задачу.<br>
Раскрывающаяся секция <b>«▶ Очередь (N)»</b> показывает содержимое очереди.
Можно выделить элементы и удалить их или очистить очередь целиком.</p>

<p><b>7. Управление загрузкой</b></p>
<ul>
  <li><b>«Скачать»</b> — запуск загрузки (или очереди)</li>
  <li><b>«Остановить»</b> — прерывает текущую загрузку и очищает очередь</li>
</ul>

<p><b>8. Прогресс и логи</b><br>
Прогресс-бар показывает процент загрузки, скорость и оставшееся время.
При загрузке нескольких потоков (видео, аудио, субтитры) отображается
прогресс текущего активного потока.<br>
Кнопки <b>«Копировать»</b> и <b>«Сохранить»</b> рядом с заголовком
логов позволяют скопировать лог в буфер обмена или сохранить в файл.</p>

<p><b>9. Проверка места на диске</b><br>
Перед каждой загрузкой проверяется, есть ли минимум 500 МБ свободного
места. Если места недостаточно, задача пропускается.</p>

<hr>

<h3>Авто-режим</h3>

<p>Галочка <b>«Авто-режим»</b> в верхней части окна включает мониторинг
буфера обмена:</p>
<ol>
  <li>Настройте место сохранения (Локально или S3, путь, профиль)</li>
  <li>Включите <b>«Авто-режим»</b></li>
  <li>Скопируйте команду N_m3u8DL-RE откуда угодно (Ctrl+C)</li>
  <li>Приложение автоматически:
    <ul>
      <li>Вставит и распарсит команду</li>
      <li>Нормализует имя файла (если включена <b>«Авто-нормализация имени»</b>)</li>
      <li>Начнёт загрузку или добавит в очередь (если загрузка уже идёт)</li>
    </ul>
  </li>
</ol>
<p>При срабатывании показывается системное уведомление (toast), чтобы
можно было работать с приложением в свёрнутом виде.</p>
<p>Реагирует <b>только</b> на текст, содержащий «N_m3u8DL-RE» —
обычное копирование не вызовет срабатывания.</p>

<hr>

<h3>Уведомления</h3>
<p>Приложение показывает системные уведомления (Windows toast) при:</p>
<ul>
  <li>Завершении очереди загрузок</li>
  <li>Ошибке загрузки (после всех повторных попыток)</li>
  <li>Ошибке отправки в S3</li>
  <li>Срабатывании авто-режима (добавление/запуск задачи)</li>
</ul>

<hr>

<h3>Вкладка «История»</h3>

<p>Все задачи сохраняются в базе данных и отображаются в таблице:</p>
<ul>
  <li><b>Имя</b> — название файла</li>
  <li><b>Статус</b> — Ожидание / Загрузка / Отправка в S3 / Готово / Ошибка</li>
  <li><b>Дата</b> — дата создания задачи</li>
  <li><b>Назначение</b> — Локально или S3</li>
</ul>

<p>Кнопки под таблицей:</p>
<ul>
  <li><b>«Показать логи»</b> — отобразить логи выбранной задачи</li>
  <li><b>«Повторить загрузку»</b> — скопировать команду и имя файла
      обратно во вкладку «Новая задача» для повторного запуска</li>
  <li><b>«Удалить»</b> — удалить задачу из истории</li>
</ul>

<hr>

<h3>Хранение данных</h3>
<p>Все настройки и история хранятся в папке
<code>~/.downloader_helper/</code>:</p>
<ul>
  <li><code>config.json</code> — настройки (пути, S3-подключение)</li>
  <li><code>tasks.db</code> — база данных истории задач (SQLite)</li>
</ul>
"""


class HelpDialog(QDialog):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowTitle("Справка")
        self.setMinimumSize(QSize(620, 500))

        layout = QVBoxLayout(self)

        browser = QTextBrowser()
        browser.setOpenExternalLinks(True)
        browser.setHtml(HELP_HTML)
        layout.addWidget(browser, 1)

        close_btn = QPushButton("Закрыть")
        close_btn.clicked.connect(self.accept)
        layout.addWidget(close_btn)
